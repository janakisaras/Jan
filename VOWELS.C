#include<stdio.h>
int main()
{
char [i]=50;
i=(a,e,i,o,u,A,E,I,O,U);
printf("\nEnter the character %c,",i)
scanf("%c",&i);
if(i)
{
printf("\nThe given character is vowel",i);
}
else(!=i)
{
printf("\nThe given number is consonant",i);
}
return 0;
}
