#include<stdio.h>
int main()
{
int a;
printf("\nEnter the number N is");
scanf("%d",&a);
a--;
if(a%2=0)

printf("\nThe given number's nearest number is even %d",a--);

a++;
elif(a%2=0)

printf("\nThe given number's nearest number is even %d",a++);

else(a%2=0)

printf("\nThe given number is equal to the even number %d");

return 0;
}
